# 架构审计续接与修复（2026-09-26）

基线：`9e7741e12c8623f6dfb49d18e6fcf619b79532dc`。本轮沿用主分支既有实现，继续处理活动羁绊、搜索正确性、账号导入与自动部署问题。

## 修复内容

1. 礼装搜索、前排选择、搜索上界统一计入当前关卡的从者自身活动加成及全队光环。候选等价归类和可替换从者提示也区分自身加成与全队光环，避免把效果不同的从者合并。
2. 条件助战礼装逐个按实际得分比较；剩余礼装上界改为每名从者分别取乐观最大值，避免交叉条件造成低估。
3. 活动或当前有效活动加成存在时不直接返回普通预计算方案；有效活动加成存在时跳过缺少完整活动状态的持久缓存。缓存版本升为 11；预计算索引版本升为 3，拒绝使用旧版本索引。
4. 穷举参考求解器接入活动参数，新增活动差分测试；旧版本可稳定复现 5312 对 5386 的次优结果，修改后通过。
5. Pages 的定时更新后部署改为检出最新 main，而非快照任务开始时的旧提交；部署包补入 generated 目录。
6. 活动关卡或羁绊数据拉取/校验失败时终止本轮发布，保留上一版，避免静默清空活动关卡。日服扩展从者拉取失败保留上一版扩展表。
7. 数据指纹补入日服扩展、别名、特性文件，避免这些文件更新未触发重建；规则版本升为 2。

## 已完成验证

- npm test 全量通过，包含新增 event-solver.test.js。
- 数据校验、求解索引校验通过。
- 现有基准测试全部成功。此基准为固定测试场景，不代表大型真实账号的最坏耗时。
- 实际登录回包解析成功；玛修羁绊上限解析为 11。以限定自有从者池及完整礼装库存进行活动配队，检查了所属从者、COST 与玛修全队 +5% 加成。
- 未将账号回包、账号凭据或账号原始数据写入仓库/交付包。

## 验证边界

这些检查不能证明所有未来机制均无缺陷。新从者和活动仍依赖上游 Atlas 数据格式；新的羁绊规则、玛修新形态或全新战斗机制可能需要扩展适配。当前活动场景为确保正确性关闭了尚未包含活动项的外层搜索上界，大型账号可能较慢。战斗模拟与完整全库存最大规模穷举不属于本次已证明的范围。

线上 Actions 与 Pages 的最终结果随本轮交付说明提供。

## 本轮交付状态

本地修复与测试完成。用户已于 2026-09-26 22:23（北京时间）在当前对话重新明确授权推送、触发 Actions、部署及测试 Pages。本提交包含本轮修复；线上部署状态以对应提交的 Actions 结果为准。

完整账号、全部从者的活动搜索在本轮等待期间未返回结果，已主动中止；不能宣称完整大账号性能测试通过。限定从者池的实际账号回归通过。

## 2026-09-27 COST 回归修正

用户指出安哥拉曼纽不是零 COST 从者，实际 COST 为 4。此前“COST 0 队伍包含玛修与安哥拉曼纽”的线上结果错误，撤回该验证结论。

根因：快照生成器在缺少 COST 字段时，把零星映射为 0，形成错误快照；计算器信任了该值。现统一 COST 解析，修正安哥拉曼纽的已有错误零值，保留玛修普通形态 0 与圣骑士形态 16；修正从者快照并重建求解索引，缓存版本升至 12。

新增回归：数据本身 COST=4、缺字段及旧零值修复、玛修形态费用、COST 0/3 排除安哥拉曼纽、COST 4 允许入队。全量 npm test 通过。

线上复测还发现资源缓存混用：HTML 已更新但 Worker/模块仍可能使用旧版本。部署时现为所有相对模块依赖及 Worker 路径统一附加提交版本号；本地 JSON 加载继承该版本。验证了入口、模块依赖和 Worker 的版本重写，避免仅修改入口查询参数却遗漏依赖。
